/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package spring.test.app;

import spring.test.list.LinkedList;

import static spring.test.utilities.StringUtils.join;
import static spring.test.utilities.StringUtils.split;
import static spring.test.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
